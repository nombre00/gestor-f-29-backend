# requirements.txt - Gestor F29 backend (actualizado marzo 2026)

# Core FastAPI y servidores
fastapi>=0.115.0
uvicorn>=0.30.0

# Base de datos y ORM
sqlalchemy>=2.0.0
pymysql                           # Driver MySQL (mysql-connector-python también sirve, pero pymysql es más liviano)
# Si más adelante cambias a SQLite para instalador local → no necesitas driver extra

# Autenticación y seguridad
passlib[bcrypt]>=1.7.4            # bcrypt se instala como extra
bcrypt==4.2.1                     # Fijo como pediste (prueba; si falla → cambia a ==4.0.1)
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9           # Para UploadFile / Form

# Modelos y settings
pydantic>=2.0.0
pydantic-settings>=2.0.0
pydantic[email]                   # Incluye email-validator para EmailStr

# Procesamiento de archivos (Excel, CSV, HTML)
pandas>=2.0.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0            # bs4
lxml>=5.0.0                       # parser rápido para bs4 (html honorarios)

# Email (invitaciones)
resend>=2.22.0                     # o la versión que uses (chequea pip show resend)

# JSON rápido (FastAPI lo usa automáticamente si está)
orjson>=3.9.0

# Opcionales pero útiles para debug / rendimiento
python-dotenv>=1.0.0              # si no usas pydantic-settings para todo